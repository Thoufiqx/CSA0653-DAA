packages = ["P1", "P2", "P3", "P4"]
weights = [10, 20, 30, 25]
profits = [60, 110, 120, 110]

capacity = 50
n = len(packages)

dp = [[0]*(capacity+1) for _ in range(n+1)]

for i in range(1, n+1):
    for w in range(capacity+1):
        if weights[i-1] <= w:
            dp[i][w] = max(
                profits[i-1] + dp[i-1][w-weights[i-1]],
                dp[i-1][w]
            )
        else:
            dp[i][w] = dp[i-1][w]

print("Optimal Profit =", dp[n][capacity])
